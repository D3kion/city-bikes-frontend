// import { INetwork, INetworkData } from './network.types';

// export const convertNetworkData = (rawData: INetworkData): INetwork[] => {
//   return rawData.networks.reduce(
//     (prev, x) => [...prev, { id: x.id, title: x.company[0] }],
//     []
//   );
// };
